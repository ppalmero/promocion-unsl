import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.promocion.unsl',
  appName: 'Promoción de carreras - UNSL',
  webDir: 'dist'
};

export default config;
